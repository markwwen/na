# 审查清单

- 输入为空、类型错误或超出范围时，行为是否明确？
- 异步操作失败、取消或超时后，资源和任务状态是否正确清理？
- 写入失败是否会留下与磁盘不一致的内存状态？
- 文件路径、符号链接和工作目录是否按预期处理？
- 请求参数是否来自当前配置，而不是旧值或硬编码？
- 历史消息、工具调用和工具结果是否保持配对？
- 现有调用方在接口变化后是否仍然正常？
- 测试是否覆盖实际失败条件，而不仅重复实现逻辑？
